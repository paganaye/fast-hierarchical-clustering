import { expect } from 'chai';
import { FasterAvgAlgorithm } from '../../src/faster/FasterAvgAlgorithm';
import { Point } from '../../src/Point';
import { AlgorithmType } from '../../src/workers/AlgorithmType';
import { getNormalizedDendrogram, runThousandQuintets, runThousandTrios } from '../Utils';

describe('Faster random tests', function () {

  it('faster algorithm is not exact. Results cannot be compared.', function () {    
  });


  
});

