import { ClusterEx, QuadNode } from './ExperimentalQuadNode';
import { Quarter } from './ExperimentalQuadTree';

export enum Sibling {
    TopLeft,
    Top,
    TopRight,
    Left,
    Self,
    Right,
    BottomLeft,
    Bottom,
    BottomRight
}

