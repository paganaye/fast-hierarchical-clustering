import { ClusterEx, QuadNode } from './FasterQuadNode';
import { Quarter } from './FasterQuadTree';

export enum Sibling {
    TopLeft,
    Top,
    TopRight,
    Left,
    Self,
    Right,
    BottomLeft,
    Bottom,
    BottomRight
}

